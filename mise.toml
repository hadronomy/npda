[tools]
"cargo:ouch" = "latest"
just = "latest"
meson = "latest"
node = "latest"

# The C++ toolchain (LLVM Clang 23) and xmake are not in the mise
# registry, so install them from the system package manager.
# See README.md for the exact steps.
